#include <windows.h>

#define IDI_APP 101

// multi-size icon group (32x32 / 48x48 / 64x64 inside app.ico);
// Windows picks the best match per context (tray / title bar / Alt+Tab)
IDI_APP ICON "app.ico"
